services:
  app:
    image: alinaeemch/rag-system
    container_name: rag-system
    env_file:
      - .env.prod
    expose:
      - "8501"
    restart: unless-stopped

  nginx:
    image: nginx:latest
    container_name: nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app
    restart: unless-stopped